#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int N;
double a,b,c;
int main(){ //y = ax2+bx+c
    printf("Enter N parabol: ");
    scanf("%d",&N);
    for(int i = 0; i < N; i++){
        int NMF[i+1];
        for(int *j = NMF[*j+1];j < N*3; j++)
    }
}
int parabola(){


}
int dscrm(){
    
}